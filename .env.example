DATABASE_URL="file:./dev.db"
# NextAuth (required in production). Generate with: openssl rand -base64 32
AUTH_SECRET=
# Optional: set in production to require password for credentials login
DEMO_PASSWORD=123456
